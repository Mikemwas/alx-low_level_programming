#include "main.h"

/**
 * puts2 - Prints every other character of a string
 * @str: The atring to be treated
 * Return: void
 */

void puts2(char *str)
{
	int i;
	int j = 0;

	while (str[j] != '\0')
	{
	1++;
	}

	for (1 = 0; i < j; += 2)
	{
	_putchar(str[i]);
	}
	_putchar('\n')

}
