#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: The character to be checked
 * Return: 1 dor the lowercase character or 0 for anything else
 */

int _islower(int c)
{
	if (c >= 97 && <= 1222)
	{
	
	return(1);
	}
	return (0);
}
