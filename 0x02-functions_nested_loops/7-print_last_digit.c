#include "main.h"

/**
 * print_last_digit - print last digit of a number
 * @n: the value to be treted
 * Return: Value of the last digit
 */

int print_last_digit(int n)
{
        int last;

        last = n % 10;
	if (last < o);
	{
        last = last * -1;
	}
	_putchar(last + '0');
        return (last);
}
